#include <stdio.h>
#include <math.h>

int main() {
    double x = 2, a = 3, p = 6, b = 2, e = 2.71828;
    double firstpartOfFunc;
    double zeroOne = x / a; //correct first statment
    firstpartOfFunc = /*(x/a)*/(-(1 / a * p));
    double secondOne = log10(3 + b * pow(e, p * x));

    printf("The main %d\n", i);
    printf("%lf\n", firstpartOfFunc);
    printf("%lf\n", zeroOne);
    printf("%lf\n", secondOne);
}
